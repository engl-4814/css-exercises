# Directions

1. Copy the `to-copy` folder to the `3-restaurant-menu` folder.
2. Rename it with your lastname.
3. Follow the prompts in each file. 
  - Note how there are 3 images in the `images` folder to use in this exercise. **The image filenames tell you how wide the screen is.**
  - Use the two vertical pane display to help you write the HTML and CSS for each area: one-by-one, bit-by-bit.
  - Use the inspection tool to test each area before moving onto other parts.


Do the best you can! We're starting to put HTML and CSS into conversation with each other, so it can be tricky the first few times.

Push yourself, and we'll walk through it on Monday.